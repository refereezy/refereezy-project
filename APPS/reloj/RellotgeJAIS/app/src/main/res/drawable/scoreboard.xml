<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo con color gris oscuro -->
    <solid android:color="#5C5F60" />

    <!-- Borde con color más claro -->
    <stroke android:width="2dp" android:color="#A0A2A3" />

    <!-- Bordes redondeados -->
    <corners android:radius="50dp" />

    <!-- Espaciado interno -->
    <padding android:left="8dp" android:right="8dp" android:top="4dp" android:bottom="4dp"/>
</shape>
